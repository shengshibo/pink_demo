var gulp = require('gulp');

// 注册任务
// gulp.task('任务名', function () {// 配置任务的操作})
// gulp.task('任务名', function () {
//   // 配置任务的操作

// });

// 注册合并压缩js的任务


// 注册默认任务
// gulp.task('default', ['任务名'])
gulp.task('default', []);